import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.diagnex.app',
  appName: 'Diagnex',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
